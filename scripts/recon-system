#!/usr/bin/env -S guix repl --
!#

(add-to-load-path (getcwd))
(use-modules
 (guix scripts time-machine)
 (scripts config))

(guix-time-machine
 "-C" channel-lock-file
 "--" "system" "reconfigure" "-L"
 dotfile-dir
 system-config)

;; Local Variables:
;; mode: scheme
;; End:
