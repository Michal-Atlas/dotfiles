;; Early Init
;; :PROPERTIES:
;; :header-args+: :tangle ~/.emacs.d/early-init.el
;; :END:

;; Don't want package.el to interfere with straight.el


;; [[file:../cl/dotfiles/Emacs.org::*Early Init][Early Init:1]]
(setq package-enable-at-startup nil)
;; Early Init:1 ends here
