#!/usr/bin/env bash
exec sudo guix system reconfigure -L "$DOTFILE_ROOT" "$0" --verbosity=3 -M4 --on-error=backtrace "${@:1}"
# shellcheck disable=SC1072
!#

(use-modules
 (rde features)
 (config))

(rde-config-operating-system
 (config-for-host (gethostname)))
