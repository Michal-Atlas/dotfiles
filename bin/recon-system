#!/usr/bin/env bash

set -euox pipefail

#shellcheck disable=SC1091
. common

pushd "$DOTFILE_ROOT"
sudo guix system reconfigure atlas/system.scm \
     --on-error=backtrace "${COMMON_FLAGS[@]}" "$@"
popd
